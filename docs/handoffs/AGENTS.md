# Handoffs Contract

This folder stores dated or historical session handoffs.

The canonical latest handoff lives at root: `SESSION_HANDOFF.md`.

When useful, agents may also copy completed handoffs into this folder using a dated filename.

Do not store secrets, tokens, or private environment values in handoff files.

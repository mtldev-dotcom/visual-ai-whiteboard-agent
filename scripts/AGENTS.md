# Scripts Contract

This folder owns project automation scripts.

Scripts must:

- Be safe to run from the repository root.
- Avoid destructive filesystem changes unless the command name and docs make that behavior explicit.
- Prefer Node.js scripts for cross-platform checks.
- Keep output concise and actionable.
- Be documented in `README.md` or the relevant implementation docs when added.
